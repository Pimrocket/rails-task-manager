<h1>My tasks:</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= task.completed %> <%= link_to task.title, task_path(task.id) %>
    <%# THIS IS THE WAY YOU WOULD WRITE IT WITHOUT USING RESOURCES IN THE ROUTER! %>
      <%# <%= link_to "Delete" delete_task_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
    <%# THIS IS THE WAY YOU WOULD WRITE IT WITHOUT USING THE ICON AS A BUTTON! %>
      <%# <%= link_to "Delete", task_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
      <%= link_to task_path(task.id), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} do %>
        <i class="fa-solid fa-trash-can"></i>
      <% end %>
    </li>
  <% end %>
</ul>
<%= link_to "New task", new_task_path %>
